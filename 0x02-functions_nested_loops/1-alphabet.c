#include "main.h"

/**
 * pint _alphabet-  prints the alphabet, in lowercase
 * with a new line
 */
void print_alphabet(void)
{
	char letter;
	for letter = 'a'; letter <= 'z'; letter++)
		_putchar(letter);

	    _putchar('\n');
}
