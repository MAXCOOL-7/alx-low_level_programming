#include <stdio.h>
/**
 * main -print alpha in lowers case, follwed by a new line
 *
 * Return: 0
 */
int main(void)
{
char ma;

for (ma = 'a'; ma <= 'z'; ma++)
{
putchar(ma);
}
putchar("\n");
return (0);
}
